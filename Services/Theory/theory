1> docker command
docker-compose up --build -d
docker-compose down
docker compose build --no-cache
docker ps -a
docker-compose -f docker-compose.yml -f docker-compose.override.yml up -d
docker compose logs discountdb
------------------------------------
for migration
D:\Development(imp)\CURRENT\github(latest)\ECommerce\Services\Ordering>dotnet ef migrations add InitialCreate -p Ordering.Infrastructure -s Ordering.API -c OrderContext


1>
Request: CreateOrderCommand arrives
         │
         ▼
┌─────────────────────────────────────┐
│ UnhandledExceptionDecorator         │  ← outermost ring
│   try {                             │
│     ┌───────────────────────────┐   │
│     │ ValidationDecorator       │   │  ← middle ring
│     │   Run validators...       │   │
│     │   If invalid → throw      │   │
│     │   ┌───────────────────┐   │   │
│     │   │ Real Handler      │   │   │  ← innermost: your actual business logic
│     │   │ CreateOrderHandler│   │   │
│     │   └───────────────────┘   │   │
│     └───────────────────────────┘   │
│   } catch(ex) { Log + rethrow }     │
└─────────────────────────────────────┘
